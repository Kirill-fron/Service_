{
    "name": "backend",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build"
    },
    "dependencies": {
        "typescript": "^5.7.3"
    }
}