{
  "name": "service_",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "b": "pnpm --filter @service_/backend",
    "w": "pnpm --filter @service_/my-react",
    "dev": "pnpm -r --parallel dev",
    "types": "pnpm b types && pnpm w types",
    "prettify": "pnpm -r --parallel prettify"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  }
}
